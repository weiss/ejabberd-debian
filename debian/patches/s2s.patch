Patch by Sergei Golovan increases timeouts in S2S.

--- ejabberd-2.0.0.orig/src/ejabberd_s2s_out.erl
+++ ejabberd-2.0.0/src/ejabberd_s2s_out.erl
@@ -90,7 +90,7 @@
 %% Only change this value if you now what your are doing:
 -define(FSMLIMITS,[]).
 %% -define(FSMLIMITS, [{max_queue, 2000}]).
--define(FSMTIMEOUT, 5000).
+-define(FSMTIMEOUT, 60000).
 
 -define(STREAM_HEADER,
 	"<?xml version='1.0'?>"
