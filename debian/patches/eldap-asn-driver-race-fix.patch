From: Evgeniy Khramtsov <ekhramtsov@process-one.net>
Subject: Load ASN.1 driver explicitly to avoid races in LDAP (EJAB-1284)
Origin: upstream, https://git.process-one.net/ejabberd/mainline/commit/c9ff37027805d1d72446b5bcbc1a60da0f2aaae4
Bug: https://support.process-one.net/browse/EJAB-1284
Bug-Debian: http://bugs.debian.org/591431

--- a/src/ejabberd_app.erl
+++ b/src/ejabberd_app.erl
@@ -57,6 +57,8 @@
     ejabberd_config:start(),
     ejabberd_check:config(),
     connect_nodes(),
+    %% Loading ASN.1 driver explicitly to avoid races in LDAP
+    catch asn1rt:load_driver(),
     Sup = ejabberd_sup:start_link(),
     ejabberd_rdbms:start(),
     ejabberd_auth:start(),
